rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // DEVELOPMENT OVERRIDE: User authenticated access
    // This allows full interaction with new protocols (Research, Learning, etc.) during testing.
    match /{document=**} {
      allow read, write: if request.auth != null;
    }
  }
}