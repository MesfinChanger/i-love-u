
rules_version = '2';

service firebase.storage {
  match /b/{bucket}/o {
    match /{allPaths=**} {
      // Testing Protocol: Full permissive access to resolve initial setup ripples.
      allow read, write: if true;
    }
  }
}
