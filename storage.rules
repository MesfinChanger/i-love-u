
rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    match /{allPaths=**} {
      // Respect Mandatory: Only authenticated hearts can share moments.
      // Granting full read/write to authenticated users for high-fidelity sharing.
      allow read, write: if request.auth != null;
    }
  }
}
