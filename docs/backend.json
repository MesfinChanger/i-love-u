{
  "entities": {
    "UserProfile": {
      "title": "User Profile",
      "description": "Dating app profile restricted to 18+. Includes relationship constraints, cultural preferences, and verified location tracking.",
      "type": "object",
      "properties": {
        "uid": { "type": "string" },
        "displayName": { "type": "string" },
        "age": { "type": "number", "minimum": 18 },
        "gender": { "type": "string", "enum": ["male", "female"] },
        "religion": { "type": "string" },
        "bio": { "type": "string" },
        "interests": { "type": "array", "items": { "type": "string" } },
        "culturalInterests": { "type": "array", "items": { "type": "string" } },
        "languagesLearning": { "type": "array", "items": { "type": "string" } },
        "photoUrl": { "type": "string" },
        "location": { "type": "string", "description": "Display location (e.g., City, Country)" },
        "exactLocation": {
          "type": "object",
          "properties": {
            "latitude": { "type": "number" },
            "longitude": { "type": "number" },
            "lastUpdated": { "type": "string", "format": "date-time" }
          }
        },
        "preferredLanguage": { "type": "string", "default": "English" },
        "relationshipStatus": { "type": "string", "enum": ["single", "dating"], "default": "single" },
        "isDatingEnabled": { "type": "boolean", "default": true },
        "partnerId": { "type": "string" },
        "publicKey": { "type": "string" },
        "isSupporter": { "type": "boolean", "default": false },
        "settings": {
          "type": "object",
          "properties": {
            "allowSensitiveContent": { "type": "boolean", "default": false },
            "shareExactLocation": { "type": "boolean", "default": true, "description": "Requirement for exclusive partners." }
          }
        },
        "preferences": {
          "type": "object",
          "properties": {
            "preferredAgeRanges": { 
              "type": "array", 
              "items": { "type": "string", "enum": ["18-33", "34-49", "50-65", "66+"] }
            }
          }
        }
      },
      "required": ["uid", "displayName", "age", "gender"]
    },
    "Match": {
      "title": "Match",
      "description": "Connection between users.",
      "type": "object",
      "properties": {
        "userIds": { "type": "array", "items": { "type": "string" } },
        "timestamp": { "type": "string", "format": "date-time" },
        "lastMessage": { "type": "string" },
        "status": { "type": "string", "enum": ["active", "unmatched"] },
        "type": { "type": "string", "enum": ["friend", "date"], "default": "friend" }
      }
    },
    "Message": {
      "title": "Message",
      "description": "Chat message. Subject to AI moderation for insults and prohibited financial/sexual solicitation.",
      "type": "object",
      "properties": {
        "senderId": { "type": "string" },
        "text": { "type": "string" },
        "encryptedText": { "type": "string" },
        "imageUrl": { "type": "string" },
        "isSensitive": { "type": "boolean", "default": false },
        "timestamp": { "type": "string", "format": "date-time" }
      }
    }
  },
  "auth": {
    "providers": ["google.com", "password", "phone"]
  },
  "firestore": {
    "/users/{userId}": {
      "schema": "UserProfile",
      "description": "User profiles with strict 18+ enforcement and verified GPS location."
    },
    "/matches/{matchId}": {
      "schema": "Match",
      "description": "Active connections."
    },
    "/matches/{matchId}/messages/{messageId}": {
      "schema": "Message",
      "description": "Moderated chat history. Prohibits solicitation."
    }
  }
}
