
/**
 * @fileOverview Universal Firebase Bridge.
 * Re-exports initialized instances for high-fidelity service interaction.
 */

import { auth, db, storage } from "@/firebase/init";

export { auth, db, storage };
