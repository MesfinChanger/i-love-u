/**
 * @fileOverview Neutralized duplicate identity.
 * Access is now exclusively managed via the route group /(protected)/identity to resolve path conflict.
 */
export default function NeutralizedPage() {
  return null;
}
