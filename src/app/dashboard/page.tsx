/**
 * @fileOverview Neutralized duplicate dashboard.
 * Access is now exclusively managed via the route group /(protected)/dashboard to resolve path conflict.
 */
export default function NeutralizedPage() {
  return null;
}
